# This is a script to convert hours to seconds

# Defined Variable
hours = 2
seconds = hours * 3600

# Calaculation for the conversion will be seconds * hours

print(f"{hours} hour(s) is {seconds} seconds")