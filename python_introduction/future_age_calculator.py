# This is a script for future age calculation

# Defined Variable
user_age = int(input("How old are you? "))
added_age = 27

# Calculated field will be age = user_age + added age
age = user_age + added_age

print(f"In 2050, you will be {age} years old.")